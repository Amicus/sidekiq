source 'http://rubygems.org'
gemspec

gem 'slim'
gem 'sprockets'
gem 'sass'
gem 'rails', '3.2.3'
gem 'sqlite3'
gem 'mongo'
gem 'bson_ext'

group :test do
  platform :mri do
    gem 'debugger'
  end
  platform :jruby do
    gem 'ruby-debug'
  end
  gem 'simplecov', :require => false
end
